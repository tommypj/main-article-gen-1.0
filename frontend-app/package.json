{
  "name": "carina-blog-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/app.js --bundle --outfile=public/app.bundle.js --loader:.js=jsx --minify --sourcemap",
    "start": "firebase serve --only hosting",
    "deploy": "firebase deploy --only hosting"
  },
  "dependencies": {
    "dompurify": "^3.2.6",
    "firebase": "^10.12.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
}
